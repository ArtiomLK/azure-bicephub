on:
  push:
    paths:
      - '.github/workflows/matrix.yml'
      - 'parameters/fd-plan-apps-appi.json'
      - 'main.bicep'
      - '!**.md'

env:
  DEPLOYMENT_TEMPLATE: main.bicep
  DEPLOYMENT_PARAMETERS: parameters/fd-plan-app-appi.json
  PROJECT_NAME: matrix

name: deploy-architecture-matrix
jobs:
  test_deploy_on_az:
    environment: dev
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: login-to-az
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: deploy-architecture-to-az
        uses: azure/arm-deploy@v1
        with:
          scope: subscription
          subscriptionId: ${{ secrets.SUB_ID }}
          deploymentName: ${{env.PROJECT_NAME}}
          region: eastus2
          template: ${{env.DEPLOYMENT_TEMPLATE}}
          parameters: ${{env.DEPLOYMENT_PARAMETERS}}
          failOnStdErr: true